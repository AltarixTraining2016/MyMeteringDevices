<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.ilich.mymeteringdevices">

    <application
        android:name=".MeteringDevicesApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.types.TypeEditActivity"
            android:label="@string/title_device_type_edit"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ui.devices.DeviceViewActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ui.devices.DeviceEditActivity"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>